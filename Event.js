class Event {
    constructor(url, title, meta) {
        this.url = url  // I think this is where the advanced URL stuff should eventually go
        this.title = title
        this.meta = meta
    }
}

module.exports = Event
